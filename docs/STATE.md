# State

Exact-source deployment automation remains declared in `portfolio/release.json`. The protected Render workflow tests an approved revision, triggers the existing service, verifies the provider source field, uploads a receipt, and requests portfolio admission.

- Lifecycle: maintained
- Deployment: live
- Exposure: anonymous
- Production claim: false
- Publication: M12 is published from `main` through the protected exact-source workflow
- Evidence: extraction, retrieval, flags, synthetic disclosure, root reachability, and private responsive browser checks

The recorded Render root check returned HTTP 200 on 2026-08-18. This is reachability evidence only. The corpus is entirely generated and fictional; the public app is not a real-client security or legal-decision system.

M12 repairs the mobile workspace without changing retrieval, citations, calibrated refusal, review flags, synthetic disclosure, or deployment configuration. Headless Chromium measured zero page-level overflow on the case, investigate, graph, timeline, audit, and evaluation routes at 390 pixels. Paired stakeholder and technical views also measured zero overflow at 1440 pixels. Axe reported zero WCAG A/AA violations; Plotly SVG contrast remains a manual incomplete check.

CI passed before the protected Render deployment verified the exact published revision and live routes. No public visibility change or paid resource was used.
