# Handoff

Read `AGENTS.md`, `README.md`, `docs/STATE.md`, the v2 manifest, and fresh Graphify output.

Next action: recreate the ignored virtual environment to remove its stale pre-move script shebang, refresh Graphify, then commit and push the validated migration on a purpose branch. Request an exact-SHA preview before updating the portfolio registry. Preserve the currently approved legacy SHA until the replacement is reviewed.

Do not promote root HTTP reachability into an availability, confidentiality, or production-readiness claim.
