# Handoff

Read `AGENTS.md`, `README.md`, `docs/STATE.md`, the v2 manifest, and fresh Graphify output.

Draft PR 1 contains the v2 migration. Next action: recreate the ignored virtual environment to remove its stale pre-move script shebang, review the PR checks, then request an exact-SHA preview before updating the portfolio registry. Preserve the currently approved legacy SHA until the replacement is reviewed.

Do not promote root HTTP reachability into an availability, confidentiality, or production-readiness claim.
