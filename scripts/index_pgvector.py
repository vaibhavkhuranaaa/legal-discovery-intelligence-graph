"""Chunk, embed, and index the processed corpus into PostgreSQL + pgvector.

NOT IMPLEMENTED YET — scheduled for the retrieval milestone
(see docs/roadmap.md). This stub exists so the repository structure and
entry points are stable from the foundation milestone onward.
"""

import sys


def main() -> int:
    print(
        "index_pgvector.py is not implemented yet. "
        "pgvector indexing arrives in the retrieval milestone (docs/roadmap.md)."
    )
    return 1


if __name__ == "__main__":
    sys.exit(main())
