"""Generate the synthetic discovery document corpus into data/raw/.

NOT IMPLEMENTED YET — scheduled for the data-generation milestone
(see docs/roadmap.md). This stub exists so the repository structure and
entry points are stable from the foundation milestone onward.
"""

import sys


def main() -> int:
    print(
        "bootstrap_data.py is not implemented yet. "
        "Synthetic corpus generation arrives in the data-generation milestone "
        "(docs/roadmap.md)."
    )
    return 1


if __name__ == "__main__":
    sys.exit(main())
