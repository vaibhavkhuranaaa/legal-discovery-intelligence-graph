"""Smoke-test the deployed application and its cloud backends.

NOT IMPLEMENTED YET — scheduled for the deployment milestone
(see docs/roadmap.md and docs/DEPLOYMENT.md). This stub exists so the
repository structure and entry points are stable from the foundation
milestone onward.
"""

import sys


def main() -> int:
    print(
        "verify_deployment.py is not implemented yet. "
        "Deployment verification arrives in the deployment milestone "
        "(docs/DEPLOYMENT.md)."
    )
    return 1


if __name__ == "__main__":
    sys.exit(main())
