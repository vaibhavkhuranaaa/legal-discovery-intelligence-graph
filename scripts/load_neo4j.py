"""Load extracted entities, documents, and events into Neo4j AuraDB.

NOT IMPLEMENTED YET — scheduled for the graph milestone
(see docs/roadmap.md). This stub exists so the repository structure and
entry points are stable from the foundation milestone onward.
"""

import sys


def main() -> int:
    print(
        "load_neo4j.py is not implemented yet. "
        "Neo4j graph loading arrives in the graph milestone (docs/roadmap.md)."
    )
    return 1


if __name__ == "__main__":
    sys.exit(main())
